const TOKEN_TYPES = {
  STRING: 'String',
  NUMBER: 'Number',
  PARENTHESIS: 'Parenthesis',
  NAME: 'Name',
};

const ERROR = {
  SYNTAX_ERROR: 'SyntaxError',
};

module.exports = { TOKEN_TYPES, ERROR };
